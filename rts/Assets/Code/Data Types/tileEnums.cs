using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// redo so that one tile can have multiple types (hash them together)
public enum tileType {
    ground, obstacle, resource, offensive, defensive, production, water, detail, empty
}